\section{Discussion}
\input{fig/ocr_undgen}
% \subsection{(what determines transferability?)}
While our experiments demonstrate that understanding-to-generation transfer can effectively improve generative capabilities, several natural questions remain. In this section, we further analyze the properties and implications of transferability in UMMs. We first investigate whether the reverse direction, \textit{i.e.}, generation-to-understanding transfer, can also provide meaningful improvements. We then examine whether jointly training understanding and generation tasks could serve as an alternative to transfer-based training. Finally, we analyze whether understanding based training negatively affects general multimodal understanding ability.

\subsection{Can generation-to-understanding transfer also be useful?}

\input{table/ocr_genund}

While the previous section demonstrated that \textit{und}~$\rightarrow$~\textit{gen} transfer can be exploited to improve specific generative capabilities, we observed that the magnitude of transferability is not uniform across tasks. In particular, the OCR results in Section~\ref{subsec:ocr} showed that understanding-to-generation transfer yields modest improvements in text rendering quality, noticeably smaller than those observed for counting and spatial reasoning. This raises the question of what factors determine how well a capability transfers across modalities. 

We hypothesize that the degree of \textit{und}~$\rightarrow$~\textit{gen} transfer may depend on the nature of the visual knowledge each task requires. Counting and spatial reasoning rely on relatively high-level, structural concepts—numerical quantity and relative object layout—that can be effectively captured through understanding alone. Text generation, by contrast, demands that the model internalize highly fine-grained visual details such as stroke geometry, character spacing, and glyph composition. Such pixel-level precision may be difficult to acquire solely through the understanding objective, which can often rely on partial or contextual cues to perform recognition without fully encoding the visual details needed for accurate rendering. This gap between what understanding learns and what generation requires may explain the weaker transfer observed for OCR. 

This reasoning leads to a natural follow-up question: if generation must encode fine-grained visual structure to produce legible text, does the reverse direction—\textit{gen}~$\rightarrow$~\textit{und} transfer—work more effectively for such tasks? To investigate this, we train Lumina-DiMOO on text generation data and evaluate its OCR recognition performance. Table~\ref{tab:ocr_transfer_genund} shows that generation-to-understanding transfer indeed yields consistent improvements across all recognition metrics, confirming that learning to generate text robustly boosts the model's capability to recognize text.  Together with the results from Section~\ref{subsec:ocr}, these findings suggest that the direction and strength of transferability are not fixed properties of the architecture alone, but can also depend on the nature of the task.

% \subsubsection{Preserving general capabilities.}
% For transfer to serve as a viable post-training strategy, task-specific understanding training must not degrade the model's existing capabilities. To verify this, we evaluate the model trained on counting understanding using general multimodal understanding benchmarks including POPE, MME, and MMBench (Table~\ref{tab:general_bench}. The results show that performance is maintained or slightly improved compared to the baseline, confirming that task-specific understanding training does not erode the model's broader knowledge.

% \vspace{0.5em}
% Taken together, these cases illustrate a general principle. Understanding training acts as a semantic regularizer that steers generation toward the target capability without corrupting the learned generative distribution. Our findings suggest that transferability is not merely an analytical observation but a viable post-training strategy.
% The preceding analysis characterizes \emph{when} transfer occurs. We now argue that transfer can be deliberately exploited: when the goal is to improve a specific generation capability, training via understanding and relying on transfer (\textit{und}\ $\rightarrow$ \textit{gen}) can be preferable to direct generation fine-tuning.

% \input{table/gen_fid}
% \paragraph{Counting: \textit{und}\ training is Pareto-dominant.}
% Table~\ref{tab:gen_quality} compares Lumina-DiMOO's counting generation under three conditions: no fine-tuning (Baseline), understanding-only training (\textit{und}\ $\rightarrow$ \textit{gen}), and generation-only training (\textit{gen}\ $\rightarrow$ \textit{gen}). Both fine-tuned conditions achieve the same task accuracy (0.57), and \textit{und}\ $\rightarrow$ \textit{gen}\ yields a lower MAD. More strikingly, image quality diverges: \textit{und}\ $\rightarrow$ \textit{gen}\ improves both Inception Score (IS) and FID over the baseline, whereas \textit{gen}\ $\rightarrow$ \textit{gen}\ degrades both metrics substantially. Understanding training thus provides the same task-level improvement at no cost to—and even a benefit for—perceptual quality, making it Pareto-dominant over direct generation training.

% \input{fig/error_decomp}
% \paragraph{Spatial position: \textit{und}\ training as a safer strategy.}
% For spatial position (Table~\ref{tab:spatial_position_transfer}), \textit{gen}\ $\rightarrow$ \textit{gen}\ is numerically stronger (81.25\% vs.\ 73.75\%). Figure~\ref{fig:error_decomp} decomposes the errors into detection failures (object not found) and position errors (object detected but misplaced). Generation training reduces both error types, whereas understanding training primarily lowers position errors while detection failures decrease only modestly. However, because the training data is synthetic (composited ImageNet crops), direct generation supervision causes the model to overfit to the synthetic distribution—generated images lose visual realism and diversity (see Figure~\ref{fig:spatial_pos}). In many practical scenarios, synthetic data is the only available supervision for generation, and training directly on it risks distribution collapse. Understanding training sidesteps this problem entirely: the model never optimizes pixel-level generation on synthetic images, so generative diversity is preserved while spatial reasoning still improves meaningfully via transfer.

% Together, these two cases illustrate a general principle: understanding training acts as a \emph{semantic regularizer} that steers generation toward the target capability without corrupting the learned generative distribution. Transferability is therefore not merely an analytical finding—it is a practical fine-tuning strategy.

\subsection{Does joint training lead to stronger transferability?}
\label{sec:joint_training}
\input{fig/joint_training}

% A natural response to the above observation is to ask: why not train on both objectives simultaneously? We find that joint \textit{und}+\textit{gen}\ training introduces \emph{seesaw interference} between the two objectives. Figure~\ref{fig:joint_training} shows Lumina-DiMOO trained with both counting understanding and generation losses over 30 epochs. During the first ${\sim}$15 epochs, understanding accuracy rises while generation accuracy declines; during the latter half, generation recovers at the expense of understanding. The two objectives oscillate in opposition, competing for representational capacity even within a low-rank LoRA adapter.

% This observation is consistent with prior analyses showing that understanding and generation demand distinct alignment patterns across network depth~\cite{li2025unifork}, and that joint optimization can lead to representational compromise. The seesaw behavior further motivates the sequential transfer strategy of Section~\ref{sec:finetune_strategy}: train on one objective, then rely on transfer for the other. This avoids representational conflict and yields stable improvements in both capabilities.

Throughout this work, we have explored transferability in UMMs and confirmed that for certain tasks, knowledge learned through one objective can boost performance in the other direction. This naturally raises the question of whether training both objectives simultaneously leads to improved performance for both.

To investigate this, we trained Lumina-DiMOO on counting understanding and generation jointly (\textit{und} + \textit{gen}), and result shown in Figure~\ref{fig:joint_training} indicate that the two objectives do not improve together during training. Instead, their performance exhibits an oscillatory pattern. Understanding accuracy increases in the first half of training while generation accuracy declines, and the trend reverses in the latter half. This behavior suggests that the presence of transferability does not necessarily translate into consistent gains when the two are optimized simultaneously.


One possible explanation is that understanding and generation require different alignment patterns within the network, which may lead to conflicting optimization signals during joint training, as suggested by~\cite{li2025unifork}. At the same time, our experimental setting may limit how clearly synergy can be observed. We conduct our experiments as post-training on a pretrained model whose capabilities are already largely established, which may lead to different training dynamics from those that arise during large-scale pre-training. In addition, the joint training uses the same concept-focused dataset constructed for the individual experiments in Section~\ref{sec:umms}, where each image–text pair is tightly aligned around \textit{counting}. Such paired data distributions are uncommon in typical multimodal pre-training corpora and may introduce an unfamiliar training configuration when the two objectives are optimized together. Distinguishing between these possibilities is difficult within the scope of post-training experiments and therefore remains an open question.

\subsection{Does understanding training harm general multimodal ability?}
\input{table/general_benchv2}

A potential concern of capability injection through understanding training is that it may negatively affect the model’s overall multimodal ability. Since our approach introduces additional supervision targeting specific capabilities, one might expect such training to overfit to the target task or degrade performance on general multimodal benchmarks.

To examine this, we evaluate the models on several widely used multimodal understanding benchmarks, including POPE \cite{li2023pope}, MMBench \cite{liu2024mmbench},MMMU \cite{yue2024mmmu}, and MME~\cite{fu2023mme}. The results are summarized in Table \ref{tab:general_bench}. Across all benchmarks, we observe that understanding-based training does not degrade general multimodal performance. The scores on POPE and MMBench remain nearly unchanged compared to the baseline model, while performance on MMMU even improves slightly after capability-focused training. These results suggest that injecting capabilities through understanding tasks introduces only minimal changes to the model parameters while preserving the model’s general multimodal knowledge. 



% \subsection{Importance of regularization: LoRA vs.\ full fine-tuning}
% \label{sec:lora_vs_full}

% The transfer results reported throughout this paper rely on LoRA fine-tuning. We now show that this choice of regularization is not incidental—it is critical. In an additional experiment with Janus-Pro, we compare LoRA fine-tuning (rank 128) against full fine-tuning of the transformer backbone (with the language modeling head frozen). Under full fine-tuning, generation collapses entirely: the model produces no coherent images despite the generation head remaining untouched. LoRA fine-tuning, by contrast, yields stable training and the transfer patterns reported in Table~\ref{tab:transfer}.

% The explanation is straightforward. LoRA constrains parameter updates to a low-rank subspace, acting as implicit regularization that preserves the pretrained generative representations. Full fine-tuning permits unconstrained parameter drift across the entire backbone, catastrophically disrupting the learned visual-generative mappings. For UMMs, regularization is not optional—it determines whether the model retains its multimodal capabilities at all.



% Taken together, our results across three tasks suggest that both the direction and the strength of transfer depend on the nature of the visual attributes a model must internalize to solve each task.


% For counting and spatial relations, the key attributes are coarse semantic signals such as object numerosity and layout. These signals tend to be encoded at a similar level regardless of whether the model is trained with an understanding-oriented or generation-oriented objective. Notably, the semantic representations learned through understanding can also provide effective guidance for generation. This observation aligns with prior work showing that improving semantic representations can directly benefit generative performance~\cite{yu2024repa, zheng2025rae}. In our experiments, a model trained to understand counting develops representations that reliably capture the number of objects in an image, and these representations remain useful when the model is asked to generate images containing a specified number of objects. Similarly, training for spatial-relation understanding encourages representations that encode relative object configurations, which can be exploited during generation to produce the intended layout. In other words, semantic knowledge acquired through understanding can meaningfully condition the generation process. When the shared concept is sufficiently coarse and can be captured at a semantic level, this conditioning effect can operate in both directions, leading to bi-directional transfer.

% In contrast, text recognition and text generation exhibit an asymmetric transfer pattern. A generation objective requires the model to synthesize individual characters with pixel-level fidelity, and the resulting fine-grained character representations transfer naturally to recognition. The reverse direction is comparatively weaker. While prior studies report that recognition training can facilitate text generation~\cite{ji2023improving, fang2025recognsyn, min2025tair}, we also observe only limited transfer from understanding to generation in this setting. A plausible explanation is that recognition-oriented training primarily emphasizes high-level textual content and identity, whereas text generation additionally demands low-level, spatially precise rendering details. Consequently, semantic knowledge extracted from recognition alone is insufficient to fully bridge the gap to pixel-accurate character synthesis. Overall, our findings indicate that when the shared concept requires fine-grained, pixel-level precision, semantic representations from understanding provide only partial support for generation.

% \subsection{Transferability as a practical fine-tuning strategy}
% \label{sec:finetune_strategy}
% From the preceding sections, we observe that training on only one modality can yield improvements in the other, producing an effect similar to training both sides simultaneously. Here, we explore whether this property can be leveraged as a practical post-training strategy. 
% In particular, our findings suggest that exploiting understanding-to-generation transfer can be practically advantageous.

% \subsubsection{Robust transfer without generation quality loss.}

% In Sections~\ref{sec:umms} and~\ref{}, we observed that understanding training effectively injects task-relevant concepts into generation for both counting and spatial relations. For counting (Table~\ref{tab:gen_quality}), understanding-only and generation-only training achieve comparable task accuracy, yet their impact on image quality diverges sharply. Understanding training (\textit{und}$\rightarrow$\textit{gen}) improves both FID and IS over the baseline, whereas generation training (\textit{gen}$\rightarrow$\textit{gen}) degrades both metrics substantially. For spatial relations (Table~\ref{tab:spatial_position_transfer}), generation training yields numerically higher accuracy (81.25\% vs.\ 73.75\%), but direct generation supervision on synthetic data causes the model to overfit to the synthetic distribution, resulting in a loss of visual realism and diversity (Figure~\ref{fig:spatial_pos}).
% \input{fig/error_decomp}
% In Sections~\ref{sec:umms} and~\ref{}, we observed that understanding training effectively injects task-relevant concepts into generation for both counting and spatial relations. For counting (Table~\ref{tab:gen_quality}), understanding-only and generation-only training achieve comparable task accuracy, yet their impact on image quality diverges sharply. Understanding training (\textit{und}$\rightarrow$ \textit{gen}) improves both FID and IS over the baseline, whereas generation training (\textit{gen}$\rightarrow$
% \textit{gen}) degrades both metrics substantially. For spatial relations (Table~\ref{tab
% :spatial_position_transfer}), generation training yields numerically higher accuracy (81.25\% vs.\ 73.75\%), but direct generation supervision on synthetic data causes the model to overfit to the synthetic distribution, resulting in a loss of visual realism and diversity (Figure~\ref{fig:spatial_pos}). Figure~\ref{fig:error_decomp} shows the error decomposition, indicating that understanding training primarily reduces position errors while detection failures remain largely unchanged, suggesting that the model's generation behavior remains close to the original distribution.
% In many practical scenarios, synthetic data may be the only available supervision for generation. However, without careful control over the data domain, post-training directly on such data risks distribution collapse. Understanding training sidesteps this problem entirely. Since the model never optimizes pixel-level generation on out-of-distribution images, generative diversity is preserved while the target concept still improves meaningfully through transfer.