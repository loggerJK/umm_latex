
\begin{figure*}[t]
    \centering
    \includegraphics[width=\linewidth]{fig/files/models_undgen.pdf} 
    \vspace{-5pt}
    \caption{\textbf{Qualitative results of count-conditioned generation across four models under three training conditions} (a) Baseline (without fine-tuning) (b) (\textit{und}$\rightarrow$
\textit{gen}) where the model is trained only on counting understanding data and evaluated on generation, and (c) (\textit{gen}$\rightarrow$ \textit{gen}) where the model is trained and evaluated on generation. We ensure each image of model is generated using same seed for fair comparison.}
    \label{fig:counting_quals}
    \vspace{-5pt}
\end{figure*}
